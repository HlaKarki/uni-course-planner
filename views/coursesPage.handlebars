<div id="page_mini_title">
    <h2>Courses Data</h2>
</div>

<div class="table_container">
    <div class="table_navbar">
        <div id="explain_to_user">
            To check the details of a course, please click the values that are <label class="aTagColor">colored</label>
        </div>
        <div class="addButton" onclick="toggleForm(`addNewCourseForm`)">
            Add new course &nbsp; &nbsp; <img src="/addIcon" alt="add icon">
        </div>
    </div>

    <table>
        <thead>
        <tr>
            <th>idCourse</th>
            <th>Title</th>
            <th>Professor</th>
            <th>Online</th>
            <th></th>
            <th></th>
        </tr>
        </thead>

        <tbody>
            {{#each courses}}
                <tr>
                    <td>{{idCourse}}</td>
                    <td><a href="/courseDetails/{{idCourse}}">{{title}}</a></td>
                    <td>{{professor}}</td>
                    {{#if online}}
                        <td>Yes</td>
                    {{else}}
                        <td>No</td>
                    {{/if}}
                    <td><a href="#updateCourseForm" onclick="updateCourse('{{idCourse}}','{{title}}', '{{description}}', '{{creditHours}}', '{{meetingTime}}', '{{idProfessor}}', '{{prerequisites}}', '{{idClassroom}}', {{online}})">Update</a></td>
                    <td><a href="#deleteCourseForm" onclick="deleteCourse('{{idCourse}}')">Delete</a></td>
                </tr>
            {{/each}}
        </tbody>

    </table>
</div>

{{>coursesPageCRUDForm professors=professors classrooms=classrooms courseNames=courseNames}}

{{>coursesPageScript}}